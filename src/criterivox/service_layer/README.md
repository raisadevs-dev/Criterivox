# Service Layer

This package composes existing Criterivox capabilities into human-useful services.

- `contracts.py`: stable service request/result/outcome contracts
- `services.py`: adapters and service implementations
- `composer.py`: capability-driven service selection/composition

Characters remain presentation/responsibility surfaces. The service layer does not create character-owned intelligence.
