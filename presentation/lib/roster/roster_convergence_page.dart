import 'package:flutter/material.dart';import '../characters/character_registry.dart';class RosterConvergencePage extends StatelessWidget{const RosterConvergencePage({super.key});@override Widget build(BuildContext c)=>Wrap(spacing:8,runSpacing:8,children:CharacterRegistry.all.map((x)=>Chip(avatar:const CircleAvatar(child:Icon(Icons.person,size:14)),label:Text('${x.name} · ${x.home}'))).toList());}
