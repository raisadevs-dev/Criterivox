import 'package:flutter/material.dart';import '../foundation/spatial_panel.dart';import '../foundation/scene.dart';class NetworkTerritory extends StatelessWidget{const NetworkTerritory({super.key});@override Widget build(BuildContext c)=>const Scene(eyebrow:'NETWORK TERRITORY',title:'Anukor',subtitle:'No conventional House.',children:[SpatialPanel(title:'SPACES',child:Text('Signal Tower · Routing Junction · Network Street · Bridge · Packet Path · Trace · Event Flow')),SpatialPanel(title:'INSPECTORS',child:Text('Context Envelope Inspector · Circuit Breaker · Protocol Bridge Inspector · Distributed Trace · Parallel Route View · Event Bus Monitor')),SpatialPanel(title:'RUNTIME TRUTH',child:Text('NOT_CONNECTED · network activity is not fabricated.'))]);}
