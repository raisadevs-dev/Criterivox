import 'package:flutter/material.dart';import '../foundation/scene.dart';import '../foundation/spatial_panel.dart';import '../runtime/capability_status.dart';import '../foundation/room_page.dart';class HomeShell extends StatelessWidget{final String title,residents;final List<String>rooms;const HomeShell({super.key,required this.title,required this.residents,required this.rooms});@override Widget build(BuildContext c)=>Scene(eyebrow:'RESPONSIBILITY DOMAIN · SHARED HOME',title:title,subtitle:'A Home is a responsibility domain. Multiple residents share it.',children:[SpatialPanel(title:'RESIDENTS',child:Text(residents)),SpatialPanel(title:'ROOMS',child:Wrap(spacing:8,runSpacing:8,children:rooms.map((r)=>ActionChip(label:Text(r),onPressed:()=>Navigator.of(c).push(MaterialPageRoute(builder:(_)=>RoomPage(title:r,description:'Semantic inspection space for this responsibility domain.',capability:CapabilityStatus.notConnected)))).toList())),const SpatialPanel(title:'HOME STATE',child:Text('NOT_CONNECTED · authoritative home read model required')),const SpatialPanel(title:'ACTIVITY',child:Text('NOT_CONNECTED · no fabricated activity')),const SpatialPanel(title:'ARTIFACTS',child:Text('NOT_CONNECTED · authoritative artifacts only')),const SpatialPanel(title:'TRACE',child:Text('NOT_CONNECTED · real runtime spans/events only'))]);}
