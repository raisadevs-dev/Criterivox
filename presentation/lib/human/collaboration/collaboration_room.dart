import 'package:flutter/material.dart';import '../../foundation/scene.dart';import '../../foundation/spatial_panel.dart';class CollaborationRoom extends StatelessWidget{const CollaborationRoom({super.key});@override Widget build(BuildContext c)=>Scene(eyebrow:'HUMAN RESIDENCE · COLLABORATION',title:'Collaboration Room',subtitle:'Shared human work with explicit roles.',children:const[SpatialPanel(title:'ROLES',child:Text('House Owner · Resident · Guest')),SpatialPanel(title:'CONTEXT MASKING WALL',child:Text('PUBLIC_TO_ROOM · RESIDENT_ONLY · OWNER_CONFIDENTIAL · GUEST_MASKED. Backend authorization remains authoritative.')),SpatialPanel(title:'CONSENSUS & DISAGREEMENT OBSERVATORY',child:Text('NOT_CONNECTED')),SpatialPanel(title:'TEAM CONTEXT STREAM',child:Text('NOT_CONNECTED')),SpatialPanel(title:'MULTI-SIGNATORY ACTION GATE',child:Text('Backend authorization required.')),SpatialPanel(title:'SHARED RESULTS JOURNAL',child:Text('Human-owned outcome recording.'))]);}}
