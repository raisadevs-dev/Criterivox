import 'package:flutter/material.dart';import '../../foundation/scene.dart';import '../../foundation/spatial_panel.dart';class GuestPassPage extends StatelessWidget{const GuestPassPage({super.key});@override Widget build(BuildContext c)=>const Scene(eyebrow:'GUEST PASS',title:'Welcome Pavilion',subtitle:'Temporary guest flow; isolation is only asserted when guaranteed by backend policy.',children:[SpatialPanel(title:'SESSION',child:Text('GUEST SESSION · ISOLATION: NOT_CONNECTED')),SpatialPanel(title:'FLOW',child:Text('Guest → Welcome Pavilion → Ephemeral Session → Goal → Data → Context → Criterivox → Inspect → Challenge → Explore → Leave / Claim Session')),SpatialPanel(title:'DECISION LOGIC X-RAY',child:Text('Inspect supported system outputs without fabricating hidden reasoning.')),SpatialPanel(title:'TRADE-OFF EXPLORER',child:Text('Trade-offs from actual analysis data only.')),SpatialPanel(title:'OWNERSHIP HANDOVER',child:Text('Backend policy + explicit human action required.'))]);}
