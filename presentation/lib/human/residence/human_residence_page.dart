import 'package:flutter/material.dart';import '../../foundation/scene.dart';import '../../foundation/spatial_panel.dart';import '../private_room/private_room.dart';import '../collaboration/collaboration_room.dart';class HumanResidencePage extends StatelessWidget{const HumanResidencePage({super.key});@override Widget build(BuildContext c)=>Scene(eyebrow:'HUMAN TERRITORY',title:'Human Residence',subtitle:'Human-owned private and collaborative decision space.',children:[SpatialPanel(title:'PRIVATE ROOM',child:FilledButton(onPressed:()=>Navigator.of(c).push(MaterialPageRoute(builder:(_)=>const PrivateRoom())),child:const Text('Enter Private Room'))),SpatialPanel(title:'COLLABORATION ROOM',child:FilledButton(onPressed:()=>Navigator.of(c).push(MaterialPageRoute(builder:(_)=>const CollaborationRoom())),child:const Text('Enter Collaboration Room')))]);}}
