import 'package:flutter/material.dart';import '../../foundation/scene.dart';import '../../foundation/spatial_panel.dart';class PrivateRoom extends StatelessWidget{const PrivateRoom({super.key});@override Widget build(BuildContext c)=>Scene(eyebrow:'HUMAN RESIDENCE · PRIVATE',title:'Private Room',subtitle:'Human remains decision authority.',children:const[SpatialPanel(title:'GOAL & CONTEXT DECOMPOSITION DESK',child:Text('Goal → Data + Context')),SpatialPanel(title:'DECISION OPTIONS OBSERVATORY',child:Text('Options, evidence and uncertainty')),SpatialPanel(title:'HUMAN CHALLENGE BENCH',child:Text('Challenge Premise · Modify Variable · Reject Option · Request Evidence · Request Counterfactual · Send to Stress Test')),SpatialPanel(title:'ACTION SAFETY GATE',child:Text('Add Constraint · Pause · Redirect · Resume · Authorize Action')),SpatialPanel(title:'RESULTS JOURNAL',child:Text('Expected → Actual → Difference → Learning')),SpatialPanel(title:'DECISION TIMELINE',child:Text('Goal → Context → Analysis → Challenge → Decision → Action → Result'))]);}}
