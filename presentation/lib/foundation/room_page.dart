import 'package:flutter/material.dart';import '../runtime/capability_status.dart';import 'spatial_panel.dart';class RoomPage extends StatelessWidget{final String title,description;final CapabilityStatus capability;const RoomPage({super.key,required this.title,required this.description,required this.capability});@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:Text(title)),body:SingleChildScrollView(padding:const EdgeInsets.all(22),child:Center(child:ConstrainedBox(constraints:const BoxConstraints(maxWidth:1100),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[SpatialPanel(title:'ROOM PURPOSE',child:Text(description)),const SizedBox(height:12),SpatialPanel(title:'CAPABILITY STATUS',child:Text(capability.label)),const SizedBox(height:12),const SpatialPanel(title:'RUNTIME TRUTH',child:Text('No fabricated telemetry, provenance, reasoning trace, authorization, evidence verification, or outcome is shown.'))])))));}
