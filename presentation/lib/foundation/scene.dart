import 'package:flutter/material.dart';class Scene extends StatelessWidget{final String eyebrow,title,subtitle;final List<Widget>children;const Scene({super.key,required this.eyebrow,required this.title,required this.subtitle,required this.children});@override Widget build(BuildContext c)=>SingleChildScrollView(padding:const EdgeInsets.all(24),child:Center(child:ConstrainedBox(constraints:const BoxConstraints(maxWidth:1400),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(eyebrow,style:const TextStyle(fontSize:10,letterSpacing:1.5,fontWeight:FontWeight.w900,color:Color(0xFF79D8C4))),const SizedBox(height:6),Text(title,style:const TextStyle(fontSize:32,fontWeight:FontWeight.w900)),const SizedBox(height:5),Text(subtitle,style:const TextStyle(color:Color(0xFF91A0B2))),const SizedBox(height:22),...children.map((x)=>Padding(padding:const EdgeInsets.only(bottom:14),child:x))]))));}
