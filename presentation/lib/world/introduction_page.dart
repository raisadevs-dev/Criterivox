import 'package:flutter/material.dart';import '../foundation/scene.dart';import '../foundation/spatial_panel.dart';import '../roster/roster_convergence_page.dart';class IntroductionPage extends StatelessWidget{const IntroductionPage({super.key});@override Widget build(BuildContext c)=>Scene(eyebrow:'WORLD ENTRY',title:'Criterivox',subtitle:'Architectural world for inspectable AI work and human decision ownership.',children:[const SpatialPanel(title:'SPATIAL ROSTER',child:RosterConvergencePage()),const SpatialPanel(title:'HIERARCHY',child:Text('World → Gate → Quarter → Home → Room → Desk / Object')),const SpatialPanel(title:'PROGRESSIVE DISCLOSURE',child:Text('Entry → Intermediate → Advanced. Disclosure depth is not an access restriction.')),const SpatialPanel(title:'TRUTH BOUNDARY',child:Text('Only authoritative runtime state may be presented as LIVE.'))]);}}
