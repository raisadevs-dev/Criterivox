import 'package:flutter/material.dart';import '../foundation/scene.dart';import '../foundation/spatial_panel.dart';import '../human/residence/human_residence_page.dart';import '../human/guest_pass/guest_pass_page.dart';class HumanProfilePage extends StatelessWidget{const HumanProfilePage({super.key});@override Widget build(BuildContext c)=>Scene(eyebrow:'GATE 2 · HUMAN CIVILIANS / PROFILE',title:'Human Residence',subtitle:'Human-owned decision and collaboration territory.',children:[SpatialPanel(title:'RESIDENCE',child:Wrap(spacing:10,children:[ActionChip(label:const Text('Human Residence'),onPressed:()=>Navigator.of(c).push(MaterialPageRoute(builder:(_)=>const HumanResidencePage()))),ActionChip(label:const Text('Guest Pass'),onPressed:()=>Navigator.of(c).push(MaterialPageRoute(builder:(_)=>const GuestPassPage())))])),const SpatialPanel(title:'LIFECYCLE',child:Text('Goal → Data + Context → Options → Human Challenge → Decision → Approval → Action → Real Result → Journal → Future Context'))]);}}
