# Fixture manifest contract

Each generated fixture records its scenario, simulated upstream component, generation configuration, output formats, seed when supplied, provenance policy, synthetic-data status, and limitations. Manifests are stable references for regression and acceptance fixtures.
