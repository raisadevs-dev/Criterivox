def generate() -> dict:
    return {"missing_context": ["structured observations", "source provenance"], "reason": "The simulated upstream request intentionally omits required context.", "synthetic": True}
